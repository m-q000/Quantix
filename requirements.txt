Django>=6.0,<7.0
psycopg2-binary>=2.9
Pillow>=10.0          # ImageField support (stall images, QR codes, violation evidence)
python-dotenv>=1.0    # .env file loading
qrcode[pil]>=7.4      # QR code generation
